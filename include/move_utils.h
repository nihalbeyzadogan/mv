#ifndef MOVE_UTILS_H
#define MOVE_UTILS_H

#include <stdbool.h>

// Dosyayı bir dizine taşır
bool move_file_to_directory(const char *source, const char *destination_dir);

#endif // MOVE_UTILS_H
